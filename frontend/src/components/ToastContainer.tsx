/**
 * ToastContainer Component
 * Displays toast notifications from EventHistoryContext
 * This is a simple component that renders notifications
 */
export const ToastContainer = () => {
  // This component will be used to display notifications from EventHistoryContext
  // The notifications are managed in the EventHistoryContext
  return null; // Notifications are now handled by EventHistoryContext
};
